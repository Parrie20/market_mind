import 'package:flutter/material.dart';

class SalesGraph extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Placeholder(
      fallbackHeight: 200,
      child: Center(
        child: Text('Sales Trends Graph Here'),
      ),
    );
  }
}
